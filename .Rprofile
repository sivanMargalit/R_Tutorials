# A fun welcome message
message("Welcome to R refresh quizes ")
# Customise the R prompt that prefixes every command
# (use " " for a blank prompt)
options(prompt = "R refresh> ")
Sys.setlocale("LC_TIME","C")  # change local date to english names
